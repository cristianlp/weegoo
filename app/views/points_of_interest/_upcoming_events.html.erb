<li class="point_of_interest event" id="event_<%= event.id %>">
  
  <%= link_to image_tag("category_#{event.category_id}.png", :style => "width: 24px;"), event %>
  <%= link_to event.name, event %>
  
  <div class="date"><%=l event.date %></div>
  
</li>