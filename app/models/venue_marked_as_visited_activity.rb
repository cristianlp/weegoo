class VenueMarkedAsVisitedActivity < Activity
  protected
  
  def broadcast
    #message = "I've visited #{self.venue.name}: #{venue_url(self.venue)}"
    #tweet(message)
    #post(message)
  end
end
