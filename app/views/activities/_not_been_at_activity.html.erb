<li class="activity">
  
  <% if current_user == activity.user_a and current_user == user %>
    You haven't been at <%= link_to activity.point_of_interest.name, activity.point_of_interest %>.
  <% else %>
    <%= link_to activity.user_a.full_name, activity.user_a %> haven't been at <%= link_to activity.point_of_interest.name, activity.point_of_interest %>.
  <% end %>
  
  <div class="information">
    <%= activity.created_at.strftime("%d %B at %H:%M") %>
  </div>
  
</li>