<%= simple_form_for([@imageable, @image]) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :image, :as => :file %>
    <%= f.input :caption %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
