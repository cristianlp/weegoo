$("ul.activities_list").append("<%= escape_javascript(render :partial => 'activities/activity', :collection => activities, :locals => { :user => user }) %>");
  
$("#pagination").replaceWith("<%= escape_javascript(paginate activities, :remote => true) %>");