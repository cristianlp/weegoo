<% title t('views.users.users_visited_places', :user => @user.full_name) %>

<%= render :partial => "users/user_information", :locals => { :user => @user } %>

<h1><%= t('views.users.visited_places').capitalize %></h1>

<%= render :partial => "users/filter_form", :locals => { :url => visited_places_path(@user) } %>

<%= render :partial => "points_of_interest/points_of_interest_#{params[:show_as] ? params[:show_as].underscore : "list" }", :locals => { :points_of_interest => @been_points_of_interest } %>

<%= link_to raw(t('views.scaffold.back')), @user %>