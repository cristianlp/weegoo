<% title t('views.events.edit') %>

<div class="form">
  
  <h1><%=t 'views.events.edit' %></h1>
  
  <%= render 'form' %>
  
  <%= link_to raw(t 'views.scaffold.back'), [@venue, @event] %>
  
</div>
